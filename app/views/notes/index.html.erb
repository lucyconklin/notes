<div class="container">
  <h1>my notes</h1>
  <%= link_to 'create a new note', new_note_path %>

  <% @notes.each do |note| %>
    <p><%= note.title %></p>
    <p><%= note.deadline %></p>

  <% end %>
</div>
